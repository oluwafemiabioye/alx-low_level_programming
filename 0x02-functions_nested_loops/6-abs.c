#include "main.h"

/**
 * _abs - Commputes the absolute value of an integerr
 * @t: The integer to be computed
 * Return: The abs 
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
